# Polymorphics:
# double dot operators overloading when the same operators are allowed to have a different meaning according to the context.

# Operators and the _functions 
# A + B has addition.          a.__add__(b)

# A - B has subtraction.        a.__sub__(b)

# A * B has multiplication.     a.__mul__(b)

# A / B has division.           a.__turediv__(b)

# A % B has addition.    a.__mod__(b)  



print( 1+2) # 3 
print(type(1))
print("apna" + " collage")
pritn(type("apna"))
print([1,2,3] + [4,5,6])
print(type([1,2,3]))